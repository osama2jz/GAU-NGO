import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
  container: {
    width: "100vw",
    height: "100vh",
    display:'flex',
  },
  form: {
    margin: "auto",
    width:'500px'
    // backgroundColor:'red'
  },
}));
